# deps + build
node_modules/
.next/
out/

# local memory + workspaces (machine-local state)
data/
.agent_state/
.agent_brain/
workspaces/

# legacy brain vault symlink (vault now lives at ./.agent_brain)
brain/
brain

# local backups (never commit)
*.bak-*

# build info
*.tsbuildinfo

# env
.env
.env*.local

# personal config (ship the example, not yours)
os.config.json

# logs
*.log
npm-debug.log*

# os
.DS_Store
