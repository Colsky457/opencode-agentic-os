# AgenticOS env overrides — every setting also lives in os.config.json.
# Copy to .env.local to use. Env wins over the config file.
# OS_HOST=127.0.0.1
# OS_PORT=3000
# BRAIN_DIR=~/brain
# CLAUDE_BIN=claude
